<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>org.marceloleite.mercado</groupId>
	<artifactId>simulator</artifactId>
	<version>1.1.0-SNAPSHOT</version>
	<packaging>jar</packaging>

	<name>simulator</name>
	<url>http://maven.apache.org</url>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>2.0.1.RELEASE</version>
	</parent>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<maven.compiler.source>1.8</maven.compiler.source>
		<maven.compiler.target>1.8</maven.compiler.target>
	</properties>

	<profiles>
		<profile>
			<id>development</id>
			<properties>
				<simulator.persistenceFile>persistence-oracle.properties</simulator.persistenceFile>
				<simulator.tradesSiteRetriever.threadPoolSize>4</simulator.tradesSiteRetriever.threadPoolSize>
				<simulator.tradesSiteRetriever.durationStep>240</simulator.tradesSiteRetriever.durationStep>
				<simulator.persistenceFileName>persistence-oracle.properties</simulator.persistenceFileName>
			</properties>
		</profile>
		<profile>
			<id>production</id>
			<properties>
				<simulator.persistenceFile>persistence-mariadb.properties</simulator.persistenceFile>
				<simulator.tradesSiteRetriever.threadPoolSize>1</simulator.tradesSiteRetriever.threadPoolSize>
				<simulator.tradesSiteRetriever.durationStep>30</simulator.tradesSiteRetriever.durationStep>
				<simulator.persistenceFileName>persistence-mariadb.properties</simulator.persistenceFileName>
			</properties>
		</profile>
	</profiles>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter</artifactId>
		</dependency>

		<dependency>
			<groupId>org.hibernate.validator</groupId>
			<artifactId>hibernate-validator</artifactId>
		</dependency>

		<dependency>
			<groupId>org.hibernate</groupId>
			<artifactId>hibernate-core</artifactId>
			<!-- <version>5.3.0.CR2</version> -->
		</dependency>


		<dependency>
			<groupId>org.marceloleite.mercado</groupId>
			<artifactId>new-model</artifactId>
			<version>0.0.1-SNAPSHOT</version>
		</dependency>

		<!-- TEST -->
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-assembly-plugin</artifactId>
				<configuration>
					<descriptorRefs>
						<descriptorRef>jar-with-dependencies</descriptorRef>
					</descriptorRefs>
					<finalName>mercado-${project.artifactId}-${project.version}</finalName>
					<appendAssemblyId>false</appendAssemblyId>
					<archive>
						<manifest>
							<mainClass>org.marceloleite.mercado.simulator.Main</mainClass>
						</manifest>
					</archive>
				</configuration>
				<executions>
					<execution>
						<id>make-assembly</id>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>true</filtering>
			</resource>
		</resources>
	</build>
</project>
